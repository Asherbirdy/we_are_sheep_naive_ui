import { defineStore } from 'pinia'

export const useProductStore = defineStore('productStore', () => {
  // ** State Data
  const state = ref({

  })
  // ** Actions

  // ** Getters

  return {
    state
  }
})